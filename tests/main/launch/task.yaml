summary: Launch workshop with pi-coding-agent SDK

prepare: |
  envsubst '${BASE}' <workshop.yaml.in >workshop.yaml
  workshop launch

restore: |
  workshop remove || true

execute: |
  workshop exec -- pi --version

debug: |
  workshop tasks
